#include<stdio.h>
int main()
{
FILE *fp;
char input[40];char input1[40];
        puts("Enter a string"); 
        gets(input); 
        puts("You entered");
        puts(input);
        puts(input);
return 0;
}
